# Add the real value in Vercel Project Settings → Environment Variables.
# Do not use the VITE_ prefix: this key must stay server-only.
OPENROUTER_API_KEY=

# Required for Gemini fallback when OpenRouter is rate-limited
GEMINI_API_KEY=
